<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <data class="SwitchBinding">

        <variable
            name="activityClass"
            type="java.lang.Class"
            />
        <variable
            name="fragment"
            type="zone.qube.databindingexample.SwitchActivityFragment"
            />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Clicking below will launch new activity"
            />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{`Launch ` + activityClass.simpleName}"
            android:onClick="@{() -> fragment.launch()}"
            tools:text="SomeActivity"
            />
    </LinearLayout>
</layout>
